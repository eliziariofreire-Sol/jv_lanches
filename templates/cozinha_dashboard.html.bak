<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>👨‍🍳 Cozinha - SOL LANCHES</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-950 text-white min-h-screen">

<!-- NAVBAR -->
<nav class="bg-black border-b border-zinc-800 sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-6 py-5 flex justify-between items-center">
        <div class="flex items-center gap-4">
            <div class="text-4xl">👨‍🍳</div>
            <div>
                <h1 class="text-3xl font-bold">SOL LANCHES</h1>
                <p class="text-zinc-400 text-sm">Painel da Cozinha</p>
            </div>
        </div>
        <a href="/cozinha/logout" 
           class="bg-red-500 hover:bg-red-600 px-6 py-3 rounded-2xl font-bold transition">
            Sair
        </a>
    </div>
</nav>

<div class="max-w-7xl mx-auto px-6 py-10">

    <h2 class="text-4xl font-bold mb-8">🚀 Pedidos para Preparar</h2>

    {% if pedidos %}
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
        {% for pedido in pedidos %}
        <div class="bg-zinc-900 rounded-3xl overflow-hidden border border-zinc-800 hover:border-emerald-500 transition">
            <div class="p-8">
                <div class="flex justify-between items-start mb-6">
                    <div>
                        <h3 class="text-2xl font-bold">#{{ pedido.id }} - {{ pedido.nome_cliente }}</h3>
                        <p class="text-zinc-400">{{ pedido.data.strftime('%d/%m/%Y às %H:%M') }}</p>
                    </div>
                    <span class="px-5 py-2 bg-orange-500 text-black rounded-full font-bold text-sm">
                        {{ pedido.status }}
                    </span>
                </div>

                <div class="bg-zinc-950 rounded-2xl p-5 mb-6">
                    <p class="font-medium text-emerald-400 mb-3">ITENS DO PEDIDO:</p>
                    <p class="text-zinc-300 leading-relaxed">{{ pedido.itens }}</p>
                </div>

                <div class="flex justify-between items-center">
                    <div>
                        <p class="text-zinc-400 text-sm">Total</p>
                        <p class="text-3xl font-bold text-emerald-400">R$ {{ pedido.total }}</p>
                    </div>
                    
                    <div class="flex gap-3">
                        <button onclick="atualizarStatus({{ pedido.id }}, 'Preparando')" 
                                class="bg-blue-600 hover:bg-blue-700 px-6 py-3 rounded-2xl font-bold transition">
                            🔄 Preparando
                        </button>
                        <button onclick="atualizarStatus({{ pedido.id }}, 'Pronto')" 
                                class="bg-emerald-500 hover:bg-emerald-600 text-black px-6 py-3 rounded-2xl font-bold transition">
                            ✅ Pronto
                        </button>
                    </div>
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
    {% else %}
    <div class="bg-zinc-900 rounded-3xl p-20 text-center">
        <p class="text-6xl mb-6">🎉</p>
        <h3 class="text-3xl font-bold mb-4">Nenhum pedido pendente</h3>
        <p class="text-zinc-400">Quando chegar novos pedidos eles aparecerão aqui.</p>
    </div>
    {% endif %}

</div>

<script>
function atualizarStatus(pedido_id, status) {
    if (confirm(`Marcar Pedido #${pedido_id} como "${status}"?`)) {
        fetch('/api/atualizar_status', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ pedido_id: pedido_id, status: status })
        })
        .then(() => location.reload());
    }
}
</script>

</body>
</html>
